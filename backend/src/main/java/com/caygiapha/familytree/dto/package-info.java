/**
 * Data transfer objects (request and response bodies) exchanged by the REST controllers. DTOs
 * carry no behavior beyond simple projection from/to entities; field-bound validation and domain
 * rules live in the service layer.
 */
package com.caygiapha.familytree.dto;
