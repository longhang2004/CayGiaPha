/**
 * Persistence layer (Spring Data JPA repositories). Repositories expose data-access methods
 * for the JPA entities mapped to the PostgreSQL schema. Entities and concrete repositories
 * are introduced alongside the schema migrations.
 */
package com.caygiapha.familytree.repository;
