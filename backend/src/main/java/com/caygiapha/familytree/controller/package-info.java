/**
 * REST controllers (web layer). Controllers translate HTTP requests into calls on the
 * domain services and map results back to JSON responses. They hold no business logic.
 */
package com.caygiapha.familytree.controller;
